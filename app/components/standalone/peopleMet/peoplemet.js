import React, {useState, useEffect} from 'react';
import {
  View,
  Text,
  StatusBar,
  TouchableOpacity,
  TextInput,
  Alert,
} from 'react-native';

export default function PeopleMetComponent({navigation}) {
  return (
    <View>
      <Text>People met screen</Text>
    </View>
  );
}
